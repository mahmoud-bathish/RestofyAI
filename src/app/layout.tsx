import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RestofyAI - Restaurant AI Solutions",
  description: "Revolutionizing restaurant operations with AI-powered solutions. Coming soon!",
  keywords: "restaurant AI, restaurant automation, AI solutions, restaurant technology",
  icons: {
    icon: '/favicon.svg',
    shortcut: '/favicon.svg',
    apple: '/restofyai-logo.png',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" dir="ltr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
